import 'package:flutter/material.dart';
import 'package:netflix/model/models.dart';

class ContentHeader extends StatelessWidget {
  
  final Content featuredContent;
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}